import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // No Turbopack — stable webpack build only
  experimental: {
    // Add experimental flags here as needed
  },
}

export default nextConfig
